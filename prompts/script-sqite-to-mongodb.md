context
MongoDB 4.4.3 est installe et test (via compass) en local.
version Nodejs  e v24.13.1
tache :
planifie la tache suivante :
créer un scrip js qui enregistre le contenu du table sqite "options"
dans le fichier database.db dans une collection "options" chaque enregistrement correspond a un document 

modifier  
scripts\sqlite-options-to-mongodb.mjs
pour enregistrer le contenu de deux tables sqite "options" et "lieux" dans deux collection "options" et "lieux" chaque enregistrement correspond a un document 
